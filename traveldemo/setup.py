#!/usr/bin/env python

from distutils.core import setup

setup(name='traveldemo',
      version='1.0',
      description='traveldemo',
      packages=['traveldemo'],
      package_dir={"traveldemo": "traveldemo"},
      entry_points={
      }, 
      install_requires=[
    ]
     )